import UIKit

enum Contact {
    enum Something {
        struct Request {
        }
        struct Response {
        }
        struct ViewModel {
        }
    }
}

struct ContactModel {
    let name: String
    let nickname: String
    let latestContactDate: String
    let imageUrl: String
}
